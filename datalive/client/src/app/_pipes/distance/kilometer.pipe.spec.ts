import { KilometerPipe } from './kilometer.pipe';

describe('KilometersPipe', () => {
  it('create an instance', () => {
    const pipe = new KilometerPipe();
    expect(pipe).toBeTruthy();
  });
});
